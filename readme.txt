
This mini-project 1 was developed my Tharun Dilliraj and Brandon Widjaja
2/25/26
github repo = https://github.com/Hellothereobi123/CMPEN_462_Proj1

the unchecked decoded mssg for the input was:" A convolUtion in the time dom`in is!multipdicat)on mn the freauency domain. "
the correct response is: "A convolution in the time domain is multiplication in the frequency domain"

Prep (no venv):
download Python 3.14.3 or higher
download VScode (optional)

make sure pip is installed, then 
    pip install numpy 
    pip install scipy 
    pip install pyspell checker (optional)
    pip install textblob (optional)


In order to to use out program, go to line 138 and 139. 
At line 138, input the path of the input.txt file you want to decode
At line 139, input the path of the preamble.txt file you want to decode 

After, on vscode, hit run. ( if you are using CMDline, cd to the program location, then type "python text_transmission_decode.py" to run" )

If you want to enable the spell check uncomment the spell check code at the very bottom